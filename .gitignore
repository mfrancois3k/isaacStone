node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Customer lead submissions — real names and phone numbers. Never commit.
# Anchored to the repo root: an unanchored "data/" also matched src/data/,
# which silently kept src/data/site.ts out of git.
/data/

.vercel
